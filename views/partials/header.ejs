<!-- views/partials/header.ejs -->
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="Content-Security-Policy" content="frame-ancestors 'self' *.hubspot.com">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Auto Finance Canada - Inventory</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Borel&family=Nunito:ital,wght@0,200..1000;1,200..1000&display=swap"
        rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <!-- Add to header.ejs -->
    <style>
        body,
        p {
            font-family: "Nunito", sans-serif;
        }

        .aspect-w-16 {
            position: relative;
            padding-bottom: 56.25%;
        }

        .aspect-w-16>* {
            position: absolute;
            height: 100%;
            width: 100%;
            top: 0;
            right: 0;
            bottom: 0;
            left: 0;
        }

        .object-cover {
            object-fit: contain;
            width: 100%;
            height: 100%;
            object-position: top;
        }

        div#iframeContainer {
            height: 85vh;
        }

        div#carModal .space-y-4>:not([hidden])~:not([hidden]) {
            margin-top: 0px !important;
        }

        div#carModal {
            z-index: 9999 !important;
        }

        .send-to-client {
            display: flex;
            gap: 10px;
        }

        .send-to-client a,
        .send-to-client button {
            display: grid;
            place-content: center;
        }

        .client-vehicle-link {
            background-color: #50C878;
        }

        .show-cars-bttn {
            background-color: #E1CA96;
        }

        /* Add keyframes for fade in from left */
        @keyframes fadeInLeft {
            0% {
                opacity: 0;
                transform: translateX(-20px);
            }

            100% {
                opacity: 1;
                transform: translateX(0);
            }
        }

        .car-inspection-status svg {
            width: 25px;
            height: 25px;
        }

        .car-inspection-status {
            opacity: 0;
            animation: fadeInLeft 0.5s ease forwards;
            animation-delay: 3s;
            z-index: 10;
            display: flex;
            justify-content: center;
        }

        /* Status icon styles */
        .status-icon-wrapper {
            position: relative;
        }

        /* Tooltip styles */
        .status-tooltip {
            position: absolute;
            bottom: 100%;
            left: 50%;
            transform: translateX(-50%);
            padding: 0.5rem 1rem;
            background-color: rgba(0, 0, 0, 0.8);
            color: white;
            font-size: 0.75rem;
            white-space: nowrap;
            border-radius: 0.25rem;
            opacity: 0;
            visibility: hidden;
            transition: all 0.2s ease;
            z-index: 50;
        }

        .status-icon-wrapper:hover .status-tooltip {
            opacity: 1;
            visibility: visible;
        }

        /* Update the container to allow overflow */
        .bg-white.rounded-lg.shadow-md {
            overflow: visible !important;
        }

        .overflow-hidden {
            overflow: visible !important;
        }

        .flex.items-center.justify-start.gap-2.car-inspection-status svg {
            width: 20px;
            height: 30px;
        }

        div#vehicleInfoContainer {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 0px 20px;
        }

        .toast {
            position: fixed;
            top: 20px;
            right: 20px;
            padding: 1rem;
            border-radius: 0.375rem;
            color: white;
            opacity: 0;
            transition: opacity 0.3s ease-in-out;
            z-index: 9999;
        }

        .toast.success {
            background-color: #10B981;
        }

        .toast.error {
            background-color: #EF4444;
        }

        .toast.show {
            opacity: 1;
        }

        div#authButton {
            z-index: 9998;
        }

        .col-span-3.flex.flex-col.justify-center.space-y-4.filter-buttons div:nth-child(2) {
            display: flex;
            justify-content: space-between;
        }

        .filter-buttons {
            margin-left: 2vw;
        }

        button#showCarsButton,
        .second-set-buttons {
            width: 89%;
        }

        form#filterForm {
            max-width: 1800px;
            margin: 0 auto;
        }

        .status-banner {
            font-size: 16px;
        }

        @media screen and (max-width: 1800px) {
            .car-name {
                text-transform: uppercase;
                width: 100%;
                text-align: center;
                min-height: 2.4em;
                line-height: 1.2;
                display: flex;
                justify-content: center;
            }
        }

        .car-name {
            text-transform: uppercase;
        }

        @media screen and (max-width: 767px) {

            button#showCarsButton,
            .second-set-buttons {
                width: 97%;
            }

            #vehicleInfoContainer {
                grid-template-columns: 1fr !important;
            }

            .filter {
                display: none !important;
                /* Hide the default filter container */
            }

            .filter.md\:block {
                display: none !important;
                /* Ensure it stays hidden on mobile */
            }

            /* Only show filter when sidebar is active */
            body.sidebar-active .filter {
                display: block !important;
                position: fixed;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                z-index: 999;
                pointer-events: none;
                background: transparent;
            }

            .filter-sidebar {
                position: fixed;
                top: 0;
                right: 0;
                bottom: 0;
                width: 100%;
                max-width: 320px;
                background: white;
                transform: translateX(100%);
                transition: transform 0.3s ease-in-out;
                z-index: 1000;
                pointer-events: auto;
                height: 100vh;
                overflow-y: auto;
                box-shadow: -2px 0 10px rgba(0, 0, 0, 0.1);
            }

            body.sidebar-active .filter-sidebar {
                transform: translateX(0);
            }

            #filterOverlay {
                position: fixed;
                inset: 0;
                background-color: rgba(0, 0, 0, 0.5);
                z-index: 998;
            }

            .filter-buttons {
                position: sticky;
                bottom: 0;
                background: white;
                margin-top: 1rem;
                border-top: 1px solid #e5e7eb;
            }

            /* Remove any fixed positioning from main filter container */
            .filter.fixed {
                position: static;
            }

            /* Ensure the main content area isn't affected by the filter overlay */
            .car-grid {
                position: relative;
                z-index: 1;
            }

            #filterForm {
                height: calc(100% - 20px);
                overflow-y: auto;
                padding-bottom: 60px;
            }

            /* Mobile header styling */
            .filter-sidebar .flex.items-center.justify-between {
                position: sticky;
                top: 0;
                background: white;
                z-index: 1001;
                padding: 1rem;
                border-bottom: 1px solid #e5e7eb;
            }

            #filterClose {
                font-size: 24px;
                width: 32px;
                height: 32px;
                display: flex;
                align-items: center;
                justify-content: center;
                border-radius: 50%;
                background: #f3f4f6;
                cursor: pointer;
            }

            #filterToggle {
                display: flex !important;
                z-index: 997;
            }

            .mobile-filters {
                padding: 10px 20px;
            }

            .filter-buttons {
                padding: 10px 0 0;
            }
        }


        /* Desktop Styles */
        @media (min-width: 769px) {
            .filter-sidebar {
                position: relative;
                transform: none !important;
                max-width: none;
                height: auto;
                width: 100%;
            }

            #filterToggle,
            #filterClose,
            #filterOverlay {
                display: none !important;
            }

            .filter-content {
                padding: 0;
            }

            .filter-footer {
                padding: 1rem 0;
                border: none;
            }
        }

        /* Common Styles */
        .filter-group {
            margin-bottom: 1rem;
        }

        .filter-group label {
            display: block;
            font-size: 0.875rem;
            font-weight: 500;
            color: #374151;
            margin-bottom: 0.25rem;
        }

        .filter-group select {
            width: 100%;
            padding: 0.5rem;
            border: 1px solid #d1d5db;
            border-radius: 0.375rem;
            background-color: white;
        }

        /* Animation Classes */
        .sidebar-enter {
            transform: translateX(100%);
        }

        .sidebar-enter-active {
            transform: translateX(0);
            transition: transform 300ms ease-in-out;
        }

        .sidebar-exit {
            transform: translateX(0);
        }

        .sidebar-exit-active {
            transform: translateX(100%);
            transition: transform 300ms ease-in-out;
        }

        /* Utility Classes */
        .h-screen {
            height: 100vh;
        }

        .overflow-hidden {
            overflow: hidden;
        }

        .filter-sidebar {
            max-width: 2000px;
            margin: 0 auto;
        }

        @media screen and (min-width: 768px) {
            .filter-sidebar {
                position: relative;
                transform: none !important;
                max-width: none;
                height: auto;
            }

            #filterToggle,
            #filterClose,
            #filterOverlay {
                display: none;
            }

            .filter-buttons {
                margin-left: 2vw;
            }

            button#showCarsButton,
            .second-set-buttons {
                width: 89%;
            }

            .second-set-buttons {
                display: flex;
                justify-content: space-between;
            }
        }

        /* Common Styles */
        .filter select {
            width: 100%;
            border: 1px solid #e5e7eb;
            border-radius: 0.375rem;
            padding: 0.5rem;
        }

        .filter label {
            display: block;
            margin-bottom: 0.25rem;
            font-size: 0.875rem;
            font-weight: 500;
            color: #374151;
        }

        /* Animation Classes */
        .sidebar-enter {
            transform: translateX(100%);
        }

        .sidebar-enter-active {
            transform: translateX(0);
            transition: transform 300ms ease-in-out;
        }

        .sidebar-exit {
            transform: translateX(0);
        }

        .sidebar-exit-active {
            transform: translateX(100%);
            transition: transform 300ms ease-in-out;
        }

        /* Car Grid Margin Adjustment */
        .car-grid {
            margin-top: 00px;
        }

        @media screen and (min-width: 1800px) {
            .car-grid {
                margin-top: 13vh !important;
            }
        }

        @media screen and (min-width: 1900px) {
            .car-grid {
                margin-top: 19vh !important;
            }
        }

        @media screen and (min-width: 768px) {
            .car-grid {
                margin-top: 18vh;
            }
        }

        @media screen and (min-width: 1000px) {
            .car-grid {
                margin-top: 24vh;
            }
        }

        @media screen and (min-width: 1200px) {
            .car-grid {
                margin-top: 23vh;
            }
        }

        @media screen and (min-width: 1400px) {
            .car-grid {
                margin-top: 20vh;
            }
        }

        @media screen and (min-width: 1600px) {
            .car-grid {
                margin-top: 22vh;
            }
        }

        button#filterToggle span {
            font-size: 1.2em;
        }
    </style>
</head>

<body class="bg-gray-100">

    <!-- views/partials/footer.ejs -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="/js/main.js" defer></script>
</body>

</html>